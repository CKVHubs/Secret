local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/REDzHUB/RedzLibV5/main/Source.Lua"))()

local Window = redzlib:MakeWindow({
  Title = "CKVHUB : Reborn a Sword 4.0.89🇵🇸",
  SubTitle = "by CKVScripter thank you so much for used it",
  SaveFolder = "testando | redz lib v5.lua"
})
local Tab1 = Window:MakeTab({"LOCK", "lock"})

Window:SelectTab(Tab1)
local Section = Tab1:AddSection({"Lock are you not subscriber"})
local Paragraph = Tab1:AddParagraph({"UPDATED 4.0.89 can't use it", " Added Hatcher, Added Player Stats, Remove Lag"})

local Button = Tab1:AddButton({"Copy Link and Subscribe 7 days", function()
  local Dialog = Window:Dialog({
    Title = "Warning",
    Text = "Past link in browse and subscribe for 70 robux or other",
    Options = {
      {"Confirm", function()
      local link = "https://www.roblox.com/game-pass/778973349/DirtDonate"
      setclipboard(link)
      end},
     
      {"Cancel", function()
        
      end}
    }
  })
end})


local Button = Tab1:AddButton({"Copy Link and Subscribe 30 days", function()
  local Dialog = Window:Dialog({
    Title = "Warning",
    Text = "Past link in browse and subscribe for 240 robux or other",
    Options = {
      {"Confirm", function()
      local link = "https://www.roblox.com/game-pass/779517767/GoldBlock"
      setclipboard(link)
      end},
     
      {"Cancel", function()
        
      end}
    }
  })
end})



local Button = Tab1:AddButton({"Copy Link and Subscribe 90 days", function()
  local Dialog = Window:Dialog({
    Title = "Warning",
    Text = "Past link in browse and subscribe for 360 robux or other",
    Options = {
      {"Confirm", function()
      local link = "https://www.roblox.com/game-pass/779683170/DiamondDonate"
      setclipboard(link)
      end},
     
      {"Cancel", function()
        
      end}
    }
  })
end})
